
def 0 {
    sound_Stop();
    dungeon_mode(25) = DMODE_OPEN;
    main_EnterDungeon(25, 30);
    switch ( message_Menu(MENU_DUNGEON_INITIALIZE_TEAM) ) { }
    main_EnterDungeon(-1, 30);
    hold;
    end;
}
